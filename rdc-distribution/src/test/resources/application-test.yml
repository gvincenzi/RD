info:
  name: RDC - Dispositivo di Distribuzione - Molecola (Test)
  artifact: '@project.artifactId@'
  version: '@project.version@'

spring:
  application:
    name: rdc-distribution
  rabbitmq:
    addresses: amqp://server
  cloud:
    stream:
      rabbit:
        bindings:
          requestChannel:
            producer:
              routingKeyExpression: "'rdcTestRequestChannel'"

rdc:
  username: test
  password: test
