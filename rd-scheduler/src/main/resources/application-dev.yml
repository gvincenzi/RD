spring:
  application:
    name: rdd-scheduler
  rabbitmq:
    addresses: amqp://server

server:
  port: 8883

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8880/eureka

template:
  entry: "Salve,\nCon questa mail si segnala il corretto inserimento del domcumento da lei proposto : \nTitolo: %s\n\nCordialmente\nMail automatica generata dal dispositivo di programmazione"
  subject:
    entry: "RDD - Conferma inserimento di un nuovo documento"

mail:
  host: smtp.test.com
  port: 587
  username: test@test.com
  password: pass

  transport:
    protocol: smtp

  smtp:
    auth: true
    starttls:
      enable: true
